

input TokenBody{
	username: String
	password: String!
}
input LogoutBody{
	access_token: String
}

type Query {
	token(body: TokenBody): undefined
	verify(body: LogoutBody): undefined
}
type Mutation{
	refreshToken(body: LogoutBody!): undefined
	logout(body: LogoutBody): undefined
}

